Constructors:
1.public ArrayList()
-->It can be used to create an empty ArrayList object with 10 elements as default capacity value.
EX: ArrayList al=new ArrayList();
2.public ArrayList(int capacity)
-->It can be used to create an empty ArrayList object with the specified capacity.
EX: ArrayList al=new ArrayList(20);
3.public ArrayList(Collection c)
-->It can be used to create an ArrayList object with all the elements of the specified Collection object.


01) import java.util.*;
class Test
{
	public static void main(String[] args) 
	{
		ArrayList al=new ArrayList();
		System.out.println(al);
	}
}

O/P:
-------
D:\packages\default>javac Test.java

D:\packages\default>java Test
[]

02)--public ArrayList(Collection c)
import java.util.*;
class Test
{
	public static void main(String[] args) 
	{
		ArrayList al1=new ArrayList();
		al1.add("AAA");
		al1.add("BBB");
		al1.add("CCC");
		al1.add("DDD");
		System.out.println(al1);
		ArrayList al2=new ArrayList(al1);
		System.out.println(al2);
	}
}

O/P:
------

D:\packages\default>javac Test.java
Note: Test.java uses unchecked or unsafe operations.
Note: Recompile with -Xlint:unchecked for details.

D:\packages\default>java Test
[AAA, BBB, CCC, DDD]
[AAA, BBB, CCC, DDD]

04)--
import java.util.*;
class Test
{
	public static void main(String[] args) 
	{
		ArrayList al1=new ArrayList();
		al1.add("AAA");
		al1.add("DDD");
		al1.add("CCC");
		al1.add("BBB");
		System.out.println(al1);
		al1.add("DDD");
		System.out.println(al1);		//DDD duplicate element will be added
		al1.add(null);
		al1.add(null);
		System.out.println(al1);		// Any no of null value can be added
		al1.add(new Integer(10));		// Any type of element is added
		al1.add(new StringBuffer("XXX"));	// Any type of element is added(Heterogenous element are added)
		System.out.println(al1);
		
	}
}

O/P:
-------

D:\packages\default>javac Test.java
Note: Test.java uses unchecked or unsafe operations.
Note: Recompile with -Xlint:unchecked for details.

D:\packages\default>java Test
[AAA, DDD, CCC, BBB]
[AAA, DDD, CCC, BBB, DDD]
[AAA, DDD, CCC, BBB, DDD, null, null]
[AAA, DDD, CCC, BBB, DDD, null, null, 10, XXX]



